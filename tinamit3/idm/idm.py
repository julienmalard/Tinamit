class IDM(object):
    def mandar(símismo, var, val):
        raise NotImplementedError

    def recibir(símismo, var):
        raise NotImplementedError

    def incrementar(símismo, n_pasos):
        raise NotImplementedError

    def finalizar(símismo):
        raise NotImplementedError

    def cerrar(símismo):
        raise NotImplementedError
