import json
import os

with open(os.path.join(os.path.dirname(os.path.realpath(__file__)), 'equiv_unid.json'), 'r', encoding='utf8') as d:
    dic_trads = json.load(d)


def convertir(de, a, val=1):

    factor =

    return factor*val